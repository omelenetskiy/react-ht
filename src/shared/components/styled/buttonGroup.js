import styled from 'styled-components';

export default styled.div`
  display: flex;
  overflow: hidden;
  align-items: center;

  .filter-buttons {
    position: relative;
    margin: 0 15px;
  }
`;
