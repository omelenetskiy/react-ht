import styled from 'styled-components';

export default styled.header`
  font-weight: bold;
  min-height: 50px;
  padding: 10px 0;
  display: inline-flex;
  justify-content: space-between;
  align-items: center;
  flex-shrink: 0;
`;
